# Daily Style Guide API

```bash
npm install
npx wrangler login
npx wrangler secret put COZE_API_TOKEN
npx wrangler deploy
```

`COZE_API_TOKEN` must be the newly rotated Coze token with workflow `run` permission. Never place it in this repository or in the H5 environment file.
